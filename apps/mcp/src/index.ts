// @jenz/mcp — thin MCP client (Remi's lane).
// Skeleton only: Phase 1 ships the contract import so this package builds.
// Remi: implement the MCP server + 4 tools against @jenz/api here.
import type { AuditedSkill } from '@jenz/shared';

export type { AuditedSkill };

console.log('@jenz/mcp skeleton — implement MCP tools against @jenz/api');
